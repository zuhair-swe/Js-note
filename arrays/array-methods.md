# Array Methods

## Common Methods

- push() → add to end
- pop() → remove from end
- shift() → remove from start
- unshift() → add to start

## Useful Methods

- map()
- filter()
- reduce()

## Example

```js
let nums = [1, 2, 3];

let doubled = nums.map(n => n * 2);